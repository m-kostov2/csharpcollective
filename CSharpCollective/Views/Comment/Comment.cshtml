@model IEnumerable<CSharpCollective.Services.DtoModels.CommentDto>;

@using Microsoft.AspNetCore.Http
@inject IHttpContextAccessor HttpContextAccessor
@{
    var userId = HttpContextAccessor.HttpContext.Session.GetString("UserId");
    var role = HttpContextAccessor.HttpContext.Session.GetString("UserRole");

}
<link rel="stylesheet" href="~/css/Comment.css" asp-append-version="true" />

<div class="main">


    <partial name="_Sidebar" />
    <div class="comments">
        <h2>Comments and suggestions</h2>

       
            <a asp-controller="Comment" asp-action="Create" class="btn btn-primary">Create New Comment</a>
        




        <div class="comments1">

            @foreach (var comment in Model)
            {
                <div class="card mb-3 p-3">
                    <p>@comment.AuthorName</p>
                    <a>@comment.Content</a>



                    @if (role == "Admin")
                    {
                        <div>
                            <p>AuthorId: @comment.AuthorId</p>
                            <a asp-controller="Comment" asp-action="Delete" asp-route-id="@comment.Id" class="btn btn-danger btn-sm">Delete</a>

                        </div>
                    }
                </div>

            }
        </div>


    </div>
</div>